---
- name: "Configure Beryl Travel Router"
  hosts: beryl
  gather_facts: false
  tasks:
    - name: "Setup Headscale on OpenWRT"
      ansible.builtin.include_role:
        name: common
        tasks_from: openwrt_headscale
