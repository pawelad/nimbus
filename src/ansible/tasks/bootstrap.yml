---
- name: "Include Common setup tasks"
  ansible.builtin.import_role:
    name: common
  tags: [common]

- name: "Include Docker setup tasks"
  ansible.builtin.import_role:
    name: docker
  tags: [docker]

- name: "Include Git deploy setup tasks"
  ansible.builtin.import_role:
    name: git_deploy
  tags: [git-deploy]
