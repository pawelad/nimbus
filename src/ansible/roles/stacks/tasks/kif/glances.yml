---
- name: "Ensure Glances stack is up to date"
  ansible.builtin.import_role:
    name: common
    tasks_from: ensure_stack
  vars:
    stack_name: glances
    env_template: "common/acme_cert.env.j2"
