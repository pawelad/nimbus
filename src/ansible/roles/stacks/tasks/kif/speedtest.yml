---
- name: "Ensure Speedtest stack is up to date"
  ansible.builtin.import_role:
    name: common
    tasks_from: ensure_stack
  vars:
    stack_name: speedtest
    env_template: "{{ inventory_hostname }}/speedtest.env.j2"
