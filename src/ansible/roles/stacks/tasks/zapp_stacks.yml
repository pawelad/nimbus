---
# Dokploy is the PaaS foundation, installed via official script
- name: "Include Dokploy tasks"
  ansible.builtin.import_tasks: zapp/dokploy.yml
  tags: [dokploy]

# Stacks
- name: "Include Beszel Agent tasks"
  ansible.builtin.import_tasks: zapp/beszel_agent.yml
  tags: [beszel-agent]

- name: "Include Headscale tasks"
  ansible.builtin.import_tasks: zapp/headscale.yml
  tags: [headscale, headplane]

- name: "Include Komodo Periphery tasks"
  ansible.builtin.import_tasks: zapp/komodo_periphery.yml
  tags: [komodo-periphery]

- name: "Include Rathole tasks"
  ansible.builtin.import_tasks: zapp/rathole.yml
  tags: [rathole]

- name: "Include Tailscale Agent tasks"
  ansible.builtin.import_tasks: zapp/tailscale_agent.yml
  tags: [tailscale-agent]
