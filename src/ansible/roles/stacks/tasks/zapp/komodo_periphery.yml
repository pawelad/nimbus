---
- name: "Allow Komodo Periphery port through UFW (Tailscale)"
  community.general.ufw:
    rule: allow
    src: "{{ tailscale_subnet }}"
    direction: in
    port: "8120"
    proto: tcp
    comment: "Komodo Periphery (Tailscale)"

- name: "Ensure Komodo Periphery stack is up to date"
  ansible.builtin.import_role:
    name: common
    tasks_from: ensure_stack
  vars:
    stack_name: komodo-periphery
    env_template: "{{ inventory_hostname }}/komodo-periphery.env.j2"
