---
- name: "Update apt cache"
  ansible.builtin.apt:
    update_cache: true

- name: "Restart Docker"
  ansible.builtin.service:
    name: docker
    state: restarted

- name: "Restart containerd"
  ansible.builtin.service:
    name: containerd
    state: restarted
